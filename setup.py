from setuptools import setup

setup(
    install_requires=[
        'colorama >= 0.4.4',
        'importlib_metadata >= 3.6',
        'packaging >= 21.3'
        # 'pkginfo >= 1.8.1',
        # 'readme_renderer >= 21.0',
        # 'requests >= 2.20',
    ],
)
